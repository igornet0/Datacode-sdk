# Example: calling functions from a native "math" module.
# If you build a native module named "math" that exports add, mul, etc.,
# place libmath.dylib (or libmath.so) where the VM can find it, then run this.
#
# import math
# println(add(2, 3))   # 5
# println(mul(4, 5))   # 20

println("Math module example: build a native 'math' module and uncomment the lines above.")
